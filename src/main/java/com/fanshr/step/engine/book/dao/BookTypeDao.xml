<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fanshr.step.engine.book.dao.BookTypeDao">


    <select id="queryList" resultType="com.fanshr.step.engine.book.entity.BookType">
        select id, title, description, level, parent_id, has_child, show_order, canceled, uuid, creator_id, create_date,
        create_time, create_at, modifier_id, modify_date, modify_time, modify_at
        from set_book_type
        <where>
            <if test="condition.Id!=null">
                and parent_id is null
            </if>
            <if test="condition.description!=null">
                and parent_id is not null
            </if>
            <if test="condition.parentId!=null">
                and parent_id = #{condition.parentId}
            </if>
            <if test="condition.title!=null">
                and title like concat('%',#{condition.title},'%')
            </if>
        </where>
        order by show_order,create_at desc,modify_at desc


    </select>
    <select id="queryOne" resultType="com.fanshr.step.engine.book.entity.BookType">
        select id, title, description, level, parent_id, has_child, show_order, canceled, uuid, creator_id, create_date,
        create_time, create_at, modifier_id, modify_date, modify_time, modify_at
        from set_book_type
        <where>
            id = #{id}
        </where>
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            keyColumn="id">
        insert into set_book_type(title, description, level, parent_id, has_child, show_order, canceled, uuid, creator_id, create_date,
                                  create_time, create_at, modifier_id, modify_date, modify_time, modify_at)
        values (#{title}, #{description}, #{level}, #{parentId}, #{hasChild},
                #{showOrder}, #{canceled}, #{uuid}, #{creatorId}, #{createDate}, #{createTime}, #{createAt},
                #{modifierId}, #{modifyDate}, #{modifyTime}, #{modifyAt})
    </insert>


    <update id="update" parameterType="com.fanshr.step.engine.book.entity.BookType">
        update set_book_type
        <set>
            <if test="title!=null">title=#{title},</if>
            <if test="description!=null">description=#{description},</if>
            <if test="level!=null">level=#{level},</if>
            <if test="hasChild!=null">has_child=#{hasChild},</if>
            <if test="canceled!=null">canceled=#{canceled},</if>
            <if test="remark!=null">remark=#{remark},</if>
            <if test="showOrder!=null">show_order=#{showOrder},</if>
            <if test="modifyAt!=null">modify_at=#{modifyAt},</if>
            <if test="parentId!=null">parent_id=#{parentId}</if>
        </set>
        where id=#{id}
    </update>
    <delete id="delete">
        delete
        from set_book_type
        where id = #{id}
    </delete>
    <delete id="batchDelete" parameterType="int">
        delete from set_book_type where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>